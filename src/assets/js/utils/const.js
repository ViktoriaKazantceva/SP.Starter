const calculation = {
	PLUS: 'plus',
	MINUS: 'minus',
};

const basketBlock = document.querySelector('.container-right-block__basket');

const amountProductConfig = {
	childList: true,
};

export { calculation, basketBlock, amountProductConfig };
